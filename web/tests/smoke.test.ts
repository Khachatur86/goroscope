/**
 * Placeholder for frontend tests.
 * Full component tests blocked by html2canvas ESM deps (ERR_REQUIRE_ESM with @csstools/css-calc).
 * See: https://github.com/vitest-dev/vitest/issues/5472
 */
import { expect, test } from "vitest";

test("smoke", () => {
  expect(1).toBe(1);
});
